let
  var a := 1
  var i := 0
in
  while i<10
  do
    (
      i := i+1;
      a := a * 2;
      if i = 5
      then
        break
    );
  printi(a);
  for n := 0 to 10
  do
  (
    a := a - 1;
    if n = 5
    then
      break
  );
  printi(a)

end
